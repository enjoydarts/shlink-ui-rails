<div class="min-h-screen bg-gradient-to-br from-blue-50 via-white to-indigo-50 py-8 px-4">
  <div class="w-full max-w-md mx-auto">
    <!-- Header Section -->
    <div class="text-center mb-8">
      <div class="inline-flex items-center justify-center w-16 h-16 bg-green-500 rounded-2xl mb-4 shadow-lg">
        <svg width="24" height="24" viewBox="0 0 24 24" class="text-white">
          <path fill="currentColor" d="M12 1L3 5V11C3 16.55 6.84 21.74 12 23C17.16 21.74 21 16.55 21 11V5L12 1M12 7C13.4 7 14.8 8.6 14.8 10V11H16V16H8V11H9.2V10C9.2 8.6 10.6 7 12 7M12 8.2C11.2 8.2 10.4 8.7 10.4 10V11H13.6V10C13.6 8.7 12.8 8.2 12 8.2Z"/>
        </svg>
      </div>
      <h1 class="text-3xl font-bold text-gray-800 mb-2">2段階認証</h1>
      <p class="text-lg text-gray-600">認証アプリで生成されたコードを入力してください</p>
    </div>

    <!-- Main Card -->
    <div class="bg-white/80 backdrop-blur-sm rounded-3xl shadow-xl border border-white/20 p-8 md:p-10 hover:shadow-2xl transition-all duration-300">
      
      <!-- Error Messages -->
      <% if flash[:alert] %>
        <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-2xl">
          <div class="flex items-center space-x-3">
            <svg class="w-5 h-5 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.962-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z"></path>
            </svg>
            <span class="text-red-700 font-medium"><%= flash[:alert] %></span>
          </div>
        </div>
      <% end %>

      <!-- User Info -->
      <div class="mb-6 p-4 bg-blue-50 border border-blue-200 rounded-2xl">
        <div class="flex items-center space-x-3">
          <svg class="w-5 h-5 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
          </svg>
          <span class="text-blue-700 font-medium"><%= @user.display_name %> としてログイン中</span>
        </div>
      </div>

      <!-- WebAuthn Authentication (if available) -->
      <% if @user.webauthn_enabled? %>
        <div class="mb-6 p-4 bg-purple-50 border border-purple-200 rounded-2xl" 
             data-controller="webauthn" 
             data-webauthn-options-url-value="<%= authentication_options_users_webauthn_credentials_path %>">
          <div class="text-center">
            <h3 class="text-lg font-semibold text-purple-800 mb-3">🔑 セキュリティキーで認証</h3>
            <p class="text-sm text-purple-700 mb-4">
              登録済みのセキュリティキーを使用して認証できます
            </p>
            <button type="button" 
                    data-action="click->webauthn#authenticate"
                    class="inline-flex items-center px-6 py-3 bg-purple-600 hover:bg-purple-700 text-white font-medium rounded-xl shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 transition-all duration-200">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z"/>
              </svg>
              セキュリティキーで認証
            </button>
            <div data-webauthn-target="errorMessage" class="hidden mt-3 p-3 bg-red-50 border border-red-200 rounded-lg">
              <p class="text-sm text-red-700"></p>
            </div>
          </div>
        </div>
        
        <div class="relative">
          <div class="absolute inset-0 flex items-center">
            <div class="w-full border-t border-gray-300"></div>
          </div>
          <div class="relative flex justify-center text-sm">
            <span class="px-2 bg-white text-gray-500">または</span>
          </div>
        </div>
      <% end %>

      <!-- 2FA Form -->
      <%= form_with url: verify_users_two_factor_authentication_path, method: :post, local: true, data: { controller: "submitter" }, class: "space-y-6" do |f| %>
        
        <!-- TOTP Code Field -->
        <div class="space-y-3">
          <%= f.label :totp_code, "🔐 認証コード", class: "block text-sm font-semibold text-gray-700 mb-3" %>
          <%= f.text_field :totp_code, 
                autofocus: true, 
                autocomplete: "one-time-code",
                placeholder: "123456",
                maxlength: 8,
                pattern: "[0-9a-zA-Z]{6,8}",
                class: "w-full px-5 py-4 rounded-2xl border-2 border-gray-200 focus:border-green-500 focus:ring-4 focus:ring-green-100 transition-all duration-200 text-base placeholder-gray-400 bg-gray-50/50 text-center font-mono text-xl tracking-widest" %>
          
          <div class="grid grid-cols-1 md:grid-cols-2 gap-3 mt-4">
            <div class="p-3 bg-gray-50 rounded-xl">
              <div class="flex items-center space-x-2">
                <svg class="w-4 h-4 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
                </svg>
                <span class="text-sm text-gray-600">Google Authenticator</span>
              </div>
            </div>
            <div class="p-3 bg-gray-50 rounded-xl">
              <div class="flex items-center space-x-2">
                <svg class="w-4 h-4 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                <span class="text-sm text-gray-600">Authy</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Submit Button -->
        <div class="pt-4">
          <button type="submit" data-submitter-target="btn"
                  class="w-full bg-green-500 hover:bg-green-600 text-white font-semibold py-3 px-6 rounded-2xl shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 transition-all duration-200 flex items-center justify-center space-x-2 disabled:opacity-60 disabled:cursor-not-allowed disabled:transform-none">
            <span data-submitter-target="label" class="text-base">🚀 認証を完了</span>
            <svg data-submitter-target="spinner" class="hidden w-5 h-5 animate-spin" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
          </button>
        </div>
      <% end %>

      <!-- Help Section -->
      <div class="mt-8 space-y-4">
        <div class="p-4 bg-yellow-50 border border-yellow-200 rounded-2xl">
          <div class="flex items-start space-x-3">
            <svg class="w-5 h-5 text-yellow-500 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <div>
              <h3 class="text-sm font-semibold text-yellow-800 mb-1">バックアップコードについて</h3>
              <p class="text-sm text-yellow-700">
                認証アプリが利用できない場合は、バックアップコードも使用できます。
                バックアップコードは8桁の英数字です。
              </p>
            </div>
          </div>
        </div>

        <div class="text-center space-y-3">
          <div class="text-sm text-gray-500">
            認証アプリが利用できませんか？
          </div>
          <details class="text-left">
            <summary class="cursor-pointer text-sm text-blue-600 hover:text-blue-800">トラブルシューティング</summary>
            <div class="mt-3 p-4 bg-gray-50 rounded-xl text-sm text-gray-600 space-y-2">
              <p>• 認証アプリの時刻が正確であることを確認してください</p>
              <p>• コードは30秒ごとに更新されます</p>
              <p>• バックアップコードは一度のみ使用可能です</p>
              <p>• 問題が続く場合は管理者にお問い合わせください</p>
            </div>
          </details>
        </div>
      </div>

      <!-- Links -->
      <div class="mt-8 text-center">
        <%= link_to "ログアウト", destroy_user_session_path, 
              method: :delete,
              class: "text-sm text-gray-500 hover:text-gray-700 transition-colors duration-200" %>
      </div>
    </div>
  </div>
</div>