<div class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 sm:p-6">
  <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center mb-4 space-y-3 sm:space-y-0">
    <div>
      <h2 class="text-lg sm:text-xl font-bold text-gray-900">🔗 Shlink API設定</h2>
      <p class="text-xs sm:text-sm text-gray-600 mt-1">短縮URL管理サーバーとの接続設定
        <span class="ml-1 sm:ml-2 px-2 py-1 text-xs bg-red-100 text-red-600 rounded-full font-medium">必須</span>
      </p>
    </div>
    <div class="flex flex-col sm:flex-row space-y-2 sm:space-y-0 sm:space-x-2">
      <button type="button" onclick="testSettings('shlink')"
              class="bg-green-600 hover:bg-green-700 text-white px-3 py-2 rounded text-xs sm:text-sm">
        接続テスト
      </button>
      <button type="button" onclick="resetCategory('shlink')"
              class="bg-gray-500 hover:bg-gray-600 text-white px-3 py-2 rounded text-xs sm:text-sm">
        リセット
      </button>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-4 sm:gap-6">
    <!-- Shlink ベースURL -->
    <div>
      <label for="shlink-base-url" class="block text-xs sm:text-sm font-medium text-gray-700 mb-2">
        ベースURL<span class="text-red-500">*</span>
      </label>
      <input type="url"
             id="shlink-base-url"
             name="settings[shlink.base_url]"
             value="<%= @settings_by_category.dig('shlink', 'shlink.base_url')&.value %>"
             placeholder="https://your-shlink.example.com"
             class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-sm">
      <p class="mt-1 text-xs sm:text-sm text-gray-600">
        Shlink APIサーバーのURL（例: https://your-shlink.example.com）
      </p>
    </div>

    <!-- API キー -->
    <div data-controller="password-toggle">
      <label for="shlink-api-key" class="block text-xs sm:text-sm font-medium text-gray-700 mb-2">
        API キー<span class="text-red-500">*</span>
      </label>
      <div class="relative">
        <input type="password"
               id="shlink-api-key"
               name="settings[shlink.api_key]"
               value="<%= @settings_by_category.dig('shlink', 'shlink.api_key')&.value %>"
               placeholder="your-shlink-api-key-here"
               data-password-toggle-target="input"
               class="w-full px-3 py-2 pr-10 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-sm">
        <button type="button" 
                data-password-toggle-target="toggleButton"
                data-action="click->password-toggle#toggle"
                aria-label="APIキーを表示"
                class="absolute inset-y-0 right-0 flex items-center pr-3 text-gray-400 hover:text-gray-600 focus:outline-none focus:text-gray-600 transition-colors duration-200">
          <!-- Show icon (eye) -->
          <svg data-password-toggle-target="showIcon" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
          </svg>
          <!-- Hide icon (eye-off) -->
          <svg data-password-toggle-target="hideIcon" class="w-5 h-5 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.543-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 114.243 4.243M9.878 9.878l4.242 4.242M9.878 9.878L3 3m6.878 6.878L15 15M21 3l-6 6"></path>
          </svg>
        </button>
      </div>
      <p class="mt-1 text-xs sm:text-sm text-gray-600">
        Shlink管理画面で生成したAPIキー
      </p>
    </div>

    <!-- タイムアウト -->
    <div>
      <label for="shlink-timeout" class="block text-xs sm:text-sm font-medium text-gray-700 mb-2">
        接続タイムアウト
      </label>
      <div class="relative">
        <input type="number"
               id="shlink-timeout"
               name="settings[shlink.timeout]"
               value="<%= @settings_by_category.dig('shlink', 'shlink.timeout')&.value %>"
               min="5"
               max="120"
               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-sm">
        <span class="absolute inset-y-0 right-0 flex items-center pr-3 text-gray-500 text-xs sm:text-sm">秒</span>
      </div>
      <p class="mt-1 text-xs sm:text-sm text-gray-600">
        API接続のタイムアウト時間（5〜120秒）
      </p>
    </div>

    <!-- リトライ回数 -->
    <div>
      <label for="shlink-retry-attempts" class="block text-xs sm:text-sm font-medium text-gray-700 mb-2">
        リトライ回数
      </label>
      <input type="number"
             id="shlink-retry-attempts"
             name="settings[shlink.retry_attempts]"
             value="<%= @settings_by_category.dig('shlink', 'shlink.retry_attempts')&.value %>"
             min="0"
             max="10"
             class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-sm">
      <p class="mt-1 text-xs sm:text-sm text-gray-600">
        接続失敗時のリトライ回数（0〜10回）
      </p>
    </div>
  </div>

  <div class="mt-4 sm:mt-6 pt-4 border-t border-gray-200">
    <p class="text-xs sm:text-sm text-gray-600">
      これらの設定はShlink APIサーバーとの通信に必要です。設定後は上部の「接続テスト」ボタンで接続確認を行ってください。
    </p>
  </div>
</div>